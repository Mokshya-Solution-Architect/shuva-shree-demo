<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="ss_pos_exchange.ControlButtons" t-inherit="point_of_sale.ControlButtons" t-inherit-mode="extension">
        <!-- Always-visible on desktop product screen -->
        <xpath expr="//SelectPartnerButton" position="after">
            <button t-if="!props.showRemainingButtons and this.pos.cashier._role != 'minimal'"
                class="btn btn-secondary btn-lg js_exchange"
                t-on-click="() => this.clickExchange()">
                <i class="fa fa-exchange me-1"/>Exchange
            </button>
        </xpath>
        <!-- Also in Actions sheet -->
        <xpath
            expr="//t[@t-if='props.showRemainingButtons']/div/button[hasclass('o_pricelist_button')]"
            position="before">
            <button t-if="this.pos.cashier._role != 'minimal'"
                class="js_exchange"
                t-att-class="buttonClass"
                t-on-click="() => this.clickExchange()">
                <i class="fa fa-exchange me-1"/>Exchange
            </button>
        </xpath>
    </t>
</templates>
